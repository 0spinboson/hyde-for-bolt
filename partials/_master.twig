<!DOCTYPE html>
<html lang="{{ htmllang() }}">

    {{ include('partials/_head.twig') }}

    <body class="theme-{{ theme.color_theme }}
        {% if theme.reverse_layout %}layout-reverse{% endif %}">

        {{ include('partials/_sidebar.twig') }}

        <div class="content container">
            {% block content %}{% endblock %}
        </div>

    </body>
</html>
